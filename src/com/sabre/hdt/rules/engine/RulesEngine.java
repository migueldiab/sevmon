package com.sabre.hdt.rules.engine;

import java.util.List;

import com.sabre.hdt.entities.Activity;
import com.sabre.hdt.rules.engine.core.RulesGroup;

public class RulesEngine {
	
	public void run(){
		/*This method should call the parser to get activities from Siebel and then instantiate each rule 
		 * and execute it.  
		 */
		
		// 1 - invocar al parser que aun no esta codificado
		// 2 - Obtener las activities de la base local y correr el conjunto de reglas a las Activities
		//List<Activity> activities = Activity.


		//rg1.execute(activity)
	}
}
